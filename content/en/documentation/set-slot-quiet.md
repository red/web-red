### set-slot-quiet
```red
USAGE:
     SET-SLOT-QUIET series value

DESCRIPTION: 
     Set a value in series without triggering eventual owner's events. 
     SET-SLOT-QUIET is a routine! value.

ARGUMENTS:
     series       [any-type!] 
     value        [any-type!] 

```