### split-path
```red
USAGE:
     SPLIT-PATH target

DESCRIPTION: 
     Splits a file or URL path. Returns a block containing path and target. 
     SPLIT-PATH is a function! value.

ARGUMENTS:
     target       [file! url!] 

```