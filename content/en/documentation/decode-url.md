### decode-url
```red
USAGE:
     DECODE-URL url

DESCRIPTION: 
     Decode a URL into an object containing its constituent parts. 
     DECODE-URL is a function! value.

ARGUMENTS:
     url          [url! string!] 

```