### load-json
```red
USAGE:
     LOAD-JSON input

DESCRIPTION: 
     Convert a JSON string to Red data. 
     LOAD-JSON is a function! value.

ARGUMENTS:
     input        [string!] "The JSON string."

```